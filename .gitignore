# build output
dist/
# generated types
.astro/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*


# environment variables
.env
.env.local
.env.production

# google service account key (used by scripts/gsc.mjs)
gsc-service-account*.json

# auth
.htpasswd

# lead pipeline — kept off GitHub entirely, because this repo is public. The
# prospecting scripts and the cold-email playbook live on this machine only.
# Their working data (scripts/leads/data/) is personal data about identifiable
# people at real businesses, and Google's Places terms cap how long some of it
# may be retained.
scripts/leads/
docs/lead-engine.md

# macOS-specific files
.DS_Store

# jetbrains setting folder
.idea/

