---
import BaseLayout from '../layouts/BaseLayout.astro';
import Img from '../components/Img.astro';
import { getCollection } from 'astro:content';

const portfolioItems = (await getCollection('portfolio'))
  .sort((a, b) => b.data.date.valueOf() - a.data.date.valueOf())
  .slice(0, 3);

const blogPosts = (await getCollection('blog', ({ data }) => !data.draft))
  .sort((a, b) => b.data.date.valueOf() - a.data.date.valueOf())
  .slice(0, 1);

const services = [
  { name: 'Branding', description: 'Identity systems that communicate your values.', href: '/services/branding' },
  { name: 'Design', description: 'Considered visual communication.', href: '/contact' },
  { name: 'Development', description: 'Fast, accessible, modern web.', href: '/contact' },
  { name: 'Marketing', description: 'Strategies that grow your reach.', href: '/contact' },
  { name: 'Environmental', description: 'Consultancy and automation for sustainable business.', href: '/services/environmental' },
];
---

<BaseLayout title="Home">
  <!-- ========== HERO ========== -->
  <section class="relative min-h-screen flex flex-col justify-center overflow-hidden">
    <div class="hero-glow"></div>

    <div class="max-w-[1400px] mx-auto w-full px-6 md:px-10 relative z-10 pt-32 pb-20 md:pt-40 md:pb-28">
      <p class="label text-forest reveal-instant mb-10 md:mb-14">
        Sustainable design studio · West Lancashire
      </p>

      <h1 class="headline text-ink max-w-4xl" style="font-size: clamp(2rem, 5.5vw, 4.5rem);">
        <span class="block reveal-instant reveal-instant-d1">We build brands that <em class="text-forest">grow</em></span>
        <span class="block reveal-instant reveal-instant-d2">without costing the <em class="text-forest">earth</em>.</span>
      </h1>

      <div class="mt-12 md:mt-16 grid md:grid-cols-12 gap-8 items-end">
        <p class="reveal-instant reveal-instant-d3 md:col-span-5 text-[15px] text-ink/60 leading-relaxed">
          Design, development, marketing and environmental consultancy —
          from a small studio with 15+ years of experience across local
          independents and international brands.
        </p>

        <div class="reveal-instant reveal-instant-d4 md:col-span-3 md:col-start-10 flex items-center justify-end">
          <a
            href="/contact"
            class="inline-flex items-center gap-3 text-[13px] font-medium text-forest group"
          >
            Start a project
            <span class="w-10 h-10 rounded-full border border-forest/30 flex items-center justify-center transition-all duration-500 group-hover:bg-forest group-hover:text-cream">
              <svg class="w-4 h-4" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
                <path d="M7 17L17 7M9 7h8v8" stroke-linecap="round" stroke-linejoin="round"/>
              </svg>
            </span>
          </a>
        </div>
      </div>
    </div>

    <!-- Scroll hint -->
    <div class="absolute bottom-8 left-1/2 -translate-x-1/2 z-10">
      <svg class="w-4 h-4 text-ink/30 nudge" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
        <path d="M12 5v14M5 12l7 7 7-7" stroke-linecap="round" stroke-linejoin="round"/>
      </svg>
    </div>
  </section>

  <!-- ========== SELECTED WORK ========== -->
  <section class="py-20 md:py-32">
    <div class="max-w-[1400px] mx-auto px-6 md:px-10">
      <div class="reveal flex items-baseline justify-between mb-12 md:mb-16">
        <p class="label text-gray">Selected work</p>
        <a href="/portfolio" class="link-line text-[13px] font-medium text-ink/60 hover:text-forest transition-colors">
          View all
        </a>
      </div>

      {portfolioItems.length > 0 ? (
        <div class="space-y-16 md:space-y-24 reveal-stagger">
          {/* Featured first item — large */}
          {portfolioItems[0] && (
            <a href={`/portfolio/${portfolioItems[0].id}`} class="reveal group block">
              <div class="img-wrap bg-stone/30 min-h-48 max-h-[500px] overflow-hidden">
                <Img
                  src={portfolioItems[0].data.thumbnail}
                  alt={portfolioItems[0].data.title}
                  loading="eager"
                  class="w-full h-auto"
                />
              </div>
              <div class="mt-5 flex items-baseline justify-between gap-4">
                <div>
                  <h3 class="headline text-xl md:text-2xl">{portfolioItems[0].data.title}</h3>
                  <p class="text-[13px] text-ink/50 mt-1">{portfolioItems[0].data.description}</p>
                </div>
                <span class="label text-gray shrink-0">{portfolioItems[0].data.category}</span>
              </div>
            </a>
          )}

          {/* Remaining items — two-up grid */}
          {portfolioItems.length > 1 && (
            <div class="grid md:grid-cols-2 gap-8 md:gap-12">
              {portfolioItems.slice(1).map((item) => (
                <a href={`/portfolio/${item.id}`} class="reveal group block">
                  <div class="img-wrap bg-stone/30 min-h-40 max-h-80 overflow-hidden">
                    <Img
                      src={item.data.thumbnail}
                      alt={item.data.title}
                      class="w-full h-auto"
                    />
                  </div>
                  <div class="mt-4 flex items-baseline justify-between gap-4">
                    <h3 class="headline text-lg md:text-xl">{item.data.title}</h3>
                    <span class="label text-gray shrink-0">{item.data.category}</span>
                  </div>
                </a>
              ))}
            </div>
          )}
        </div>
      ) : (
        <p class="text-ink/50 text-center py-12">Work coming soon.</p>
      )}
    </div>
  </section>

  <!-- ========== APPROACH ========== -->
  <section class="py-20 md:py-32 border-t border-stone">
    <div class="max-w-[1400px] mx-auto px-6 md:px-10">
      <div class="grid md:grid-cols-12 gap-12 md:gap-16">
        <div class="reveal md:col-span-5">
          <p class="label text-gray mb-6">Our approach</p>
          <h2 class="headline text-2xl md:text-3xl">
            Good design and good ethics aren't at&nbsp;odds.
          </h2>
          <p class="mt-6 text-[15px] text-ink/60 leading-relaxed">
            Every project we take on is shaped by a simple belief: brands that
            respect the planet perform better, last longer, and matter more.
            We weave sustainability into every decision — from supply chain
            consultancy to the code we ship.
          </p>
        </div>

        <div class="reveal md:col-span-6 md:col-start-7">
          <p class="label text-gray mb-6">Services</p>
          <ul class="border-t border-stone">
            {services.map((service) => (
              <li class="border-b border-stone">
                <a href={service.href} class="group flex items-baseline justify-between py-4 md:py-5 gap-4">
                  <span class="text-[15px] md:text-base font-medium text-ink group-hover:text-forest transition-colors duration-300">
                    {service.name}
                  </span>
                  <span class="text-[13px] text-ink/40 hidden sm:block">
                    {service.description}
                  </span>
                </a>
              </li>
            ))}
          </ul>
        </div>
      </div>

      <!-- Stats -->
      <div class="reveal mt-20 md:mt-28 grid grid-cols-3 gap-6 md:gap-12 border-t border-stone pt-10">
        {[
          { n: '15+', label: 'Years experience' },
          { n: '120+', label: 'Projects delivered' },
          { n: 'Est. 2025', label: 'West Lancashire, UK' },
        ].map((stat) => (
          <div>
            <div class="headline text-2xl md:text-3xl">{stat.n}</div>
            <div class="label text-gray mt-2">{stat.label}</div>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- ========== JOURNAL ========== -->
  {blogPosts.length > 0 && (
    <section class="py-16 md:py-20 border-t border-stone">
      <div class="max-w-[1400px] mx-auto px-6 md:px-10">
        <div class="reveal flex items-center justify-between gap-6">
          <div class="flex items-baseline gap-6 md:gap-10 min-w-0">
            <p class="label text-gray shrink-0">Latest</p>
            <a href={`/blog/${blogPosts[0].id}`} class="group flex items-baseline gap-4 min-w-0">
              <h3 class="text-[15px] md:text-base font-medium text-ink group-hover:text-forest transition-colors truncate">
                {blogPosts[0].data.title}
              </h3>
              <time class="label text-gray shrink-0 hidden sm:block">
                {blogPosts[0].data.date.toLocaleDateString('en-GB', { day: '2-digit', month: 'short', year: 'numeric' })}
              </time>
            </a>
          </div>
          <a href="/blog" class="link-line text-[13px] font-medium text-ink/60 hover:text-forest transition-colors shrink-0">
            All posts
          </a>
        </div>
      </div>
    </section>
  )}

  <!-- ========== CTA ========== -->
  <section class="py-24 md:py-40 border-t border-stone relative overflow-hidden">
    <div class="hero-glow" style="top: 50%;"></div>
    <div class="max-w-[1400px] mx-auto px-6 md:px-10 relative z-10">
      <div class="reveal max-w-2xl">
        <p class="label text-gray mb-6">Ready when you are</p>
        <h2 class="headline text-3xl md:text-4xl lg:text-5xl">
          Let's build something<br/>that <em class="text-forest">lasts</em>.
        </h2>
        <div class="flex flex-wrap gap-4 mt-10">
          <a
            href="/book"
            class="inline-flex items-center gap-3 text-[13px] font-medium text-forest group"
          >
            Book a free call
            <span class="w-10 h-10 rounded-full border border-forest/30 flex items-center justify-center transition-all duration-500 group-hover:bg-forest group-hover:text-cream">
              <svg class="w-4 h-4" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
                <path d="M7 17L17 7M9 7h8v8" stroke-linecap="round" stroke-linejoin="round"/>
              </svg>
            </span>
          </a>
          <a
            href="/contact"
            class="inline-flex items-center gap-3 text-[13px] font-medium text-ink/70 hover:text-forest transition-colors duration-300"
          >
            Send a message
          </a>
        </div>
      </div>
    </div>
  </section>
</BaseLayout>
